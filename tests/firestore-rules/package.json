{
  "name": "tierdex-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Local baseline tests for Tierdex Firestore security rules.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.json --project tierdex-rules-test --only firestore \"node --test firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1"
  }
}
